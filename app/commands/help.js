const Server = require('../controllers/server');

module.exports = {
    name: 'help',
    description: 'Return list of commands available for use',
    execute(message) {
        // Do something
    },
};
